import Link from "next/link";import {PageHero} from "@/components/page-hero";import {posts} from "@/lib/data";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <><PageHero eyebrow="Insights" title="Trade intelligence for two markets" text="Practical guidance on sourcing, exporting, compliance and logistics."/><section className="section"><div className="container-shell grid gap-6">{posts.map(p=><article className="card p-8" key={p.slug}><time className="text-xs uppercase tracking-widest text-ink/40">{p.date}</time><h2 className="mt-3 text-2xl font-semibold">{p.title[locale]}</h2><p className="mt-3 text-ink/60">Actionable insights for companies trading across Türkiye and Chile.</p><Link href={`/${locale}/blog/${p.slug}`} className="mt-5 inline-block font-semibold text-copper">Read article →</Link></article>)}</div></section></>}