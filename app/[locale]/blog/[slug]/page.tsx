import {notFound} from "next/navigation";import {posts} from "@/lib/data";import {isLocale} from "@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string,slug:string}>}){const {locale,slug}=await params;if(!isLocale(locale))notFound();const p=posts.find(x=>x.slug===slug);if(!p)notFound();return <article className="section"><div className="container-shell max-w-3xl"><p className="eyebrow">HKO Blog · {p.date}</p><h1 className="heading">{p.title[locale]}</h1><div className="mt-10 space-y-6 text-lg leading-9 text-ink/70"><p>Successful cross-border trade begins with a precise commercial requirement and verified counterparties.</p><p>Companies should evaluate specifications, certifications, Incoterms, payment risk, freight conditions and destination compliance before confirming an order.</p><p>HKO Trade Hub supports this process with local coordination in Türkiye and Chile.</p></div></div></article>}
