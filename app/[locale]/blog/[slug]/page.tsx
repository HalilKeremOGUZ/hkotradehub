import {notFound} from "next/navigation";import {posts} from "@/lib/data";import {isLocale} from "@/lib/i18n";
const body={tr:["Başarılı sınır ötesi ticaret, kesin biçimde tanımlanmış bir ticari ihtiyaç ve doğrulanmış iş ortaklarıyla başlar.","Şirketler siparişi onaylamadan önce teknik özellikleri, sertifikaları, Incoterms koşullarını, ödeme riskini, navlun şartlarını ve hedef ülke mevzuatını değerlendirmelidir.","HKO Trade Hub bu süreci Türkiye ve Şili’deki yerel koordinasyonuyla destekler."],en:["Successful cross-border trade begins with a precise commercial requirement and verified counterparties.","Companies should evaluate specifications, certifications, Incoterms, payment risk, freight conditions and destination compliance before confirming an order.","HKO Trade Hub supports this process with local coordination in Turkey and Chile."],es:["El comercio internacional exitoso comienza con una necesidad comercial precisa y contrapartes verificadas.","Antes de confirmar un pedido, las empresas deben evaluar especificaciones, certificaciones, Incoterms, riesgo de pago, condiciones de transporte y cumplimiento en el país de destino.","HKO Trade Hub apoya este proceso mediante coordinación local en Turquía y Chile."]} as const;
export default async function Page({params}:{params:Promise<{locale:string,slug:string}>}){const {locale,slug}=await params;if(!isLocale(locale))notFound();const p=posts.find(x=>x.slug===slug);if(!p)notFound();return <article className="section"><div className="container-shell max-w-3xl"><p className="eyebrow">HKO Blog · {p.date}</p><h1 className="heading">{p.title[locale]}</h1><div className="mt-10 space-y-6 text-lg leading-9 text-ink/70">{body[locale].map(x=><p key={x}>{x}</p>)}</div></div></article>}
