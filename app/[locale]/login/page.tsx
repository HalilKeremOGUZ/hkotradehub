"use client";import {useState} from "react";import {useParams,useRouter} from "next/navigation";export default function Login(){const [error,setError]=useState("");const router=useRouter();const p=useParams<{locale:string}>();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(r.ok)router.push(`/${p.locale}/admin`);else setError("Invalid credentials")};return <section className="section bg-sand"><div className="container-shell max-w-md"><form onSubmit={submit} className="card grid gap-4 p-8"><h1 className="text-3xl font-semibold">Admin Login</h1><input className="input" name="email" type="email" required placeholder="Email"/><input className="input" name="password" type="password" required placeholder="Password"/><button className="btn-primary">Sign in</button>{error&&<p className="text-sm text-red-600">{error}</p>}</form></div></section>}