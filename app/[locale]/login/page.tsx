"use client";import {useState} from "react";import {useParams,useRouter} from "next/navigation";import type {Locale} from "@/lib/i18n";
const copy={tr:{title:"Yönetici Girişi",email:"E-posta",password:"Şifre",button:"Giriş yap",error:"Giriş bilgileri geçersiz"},en:{title:"Admin Login",email:"Email",password:"Password",button:"Sign in",error:"Invalid credentials"},es:{title:"Acceso de Administrador",email:"Correo electrónico",password:"Contraseña",button:"Ingresar",error:"Credenciales inválidas"}} as const;
export default function Login(){const [error,setError]=useState("");const router=useRouter();const p=useParams<{locale:Locale}>();const t=copy[p.locale]||copy.en;async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(r.ok)router.push(`/${p.locale}/admin`);else setError(t.error)}return <section className="section bg-sand"><div className="container-shell max-w-md"><form onSubmit={submit} className="card grid gap-4 p-8"><h1 className="text-3xl font-semibold">{t.title}</h1><label className="grid gap-2"><span>{t.email}</span><input aria-label={t.email} className="input" name="email" type="email" required placeholder={t.email}/></label><label className="grid gap-2"><span>{t.password}</span><input aria-label={t.password} className="input" name="password" type="password" required placeholder={t.password}/></label><button className="btn-primary">{t.button}</button>{error&&<p role="alert" className="text-sm text-red-600">{error}</p>}</form></div></section>}
