import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {products} from "@/lib/data";import {isLocale} from "@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string,slug:string}>}){const {locale,slug}=await params;if(!isLocale(locale))notFound();const p=products.find(x=>x.slug===slug);if(!p)notFound();return <section className="section"><div className="container-shell grid gap-12 lg:grid-cols-2"><div className="relative min-h-[480px] overflow-hidden rounded-[2.5rem]"><Image src={p.image} alt={p.names[locale]} fill className="object-cover"/></div><div className="self-center"><p className="eyebrow">{p.category} · {p.origin}</p><h1 className="heading">{p.names[locale]}</h1><p className="mt-6 text-lg leading-8 text-ink/60">{p.desc[locale]}</p><div className="mt-8 grid gap-3 rounded-3xl bg-sand p-6 text-sm"><p><b>Trade support:</b> sourcing, verification, logistics and documentation.</p><p><b>Availability:</b> subject to producer capacity and destination requirements.</p><p><b>Commercial terms:</b> provided after requirement review.</p></div><Link href={`/${locale}/quote?product=${p.slug}`} className="btn-primary mt-8">Request a tailored quote</Link></div></div></section>}