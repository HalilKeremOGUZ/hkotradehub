import { ProductCard } from "@/components/product-card"; import { PageHero } from "@/components/page-hero"; import { products } from "@/lib/data"; import { isLocale } from "@/lib/i18n"; import { notFound } from "next/navigation"; export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <><PageHero eyebrow="Marketplace" title="Selected trade opportunities" text="Explore product categories sourced from verified business networks in Türkiye and Chile."/><section className="section"><div className="container-shell grid gap-7 md:grid-cols-2 lg:grid-cols-3">{products.map(p=><ProductCard product={p} locale={locale} key={p.slug}/>)}</div></section></>}