import {NextResponse} from "next/server";import {z} from "zod";import {prisma} from "@/lib/prisma";const s=z.object({name:z.string().min(2),company:z.string().optional(),email:z.string().email(),phone:z.string().optional(),country:z.string().optional(),subject:z.string().optional(),message:z.string().min(10),type:z.enum(["CONTACT","SUPPLIER","BUYER"]).default("CONTACT")});export async function POST(req:Request){try{const d=s.parse(await req.json());await prisma.inquiry.create({data:d});return NextResponse.json({ok:true},{status:201})}catch{return NextResponse.json({error:"Unable to save inquiry"},{status:400})}}