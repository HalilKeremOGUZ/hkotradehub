"use client";
import { useState } from "react";
export function LeadForm({type="CONTACT",productSlug}:{type?:string,productSlug?:string}){const [state,setState]=useState<"idle"|"loading"|"success"|"error">("idle");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("loading");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch(type==="QUOTE"?"/api/quotes":"/api/inquiries",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...data,type,productSlug})});setState(res.ok?"success":"error");if(res.ok)e.currentTarget.reset()}return <form onSubmit={submit} className="card grid gap-4 p-7 lg:p-9"><div className="grid gap-4 sm:grid-cols-2"><input name="name" required placeholder="Name / Nombre / Ad Soyad" className="input"/><input name="company" required placeholder="Company / Empresa / Firma" className="input"/></div><div className="grid gap-4 sm:grid-cols-2"><input name="email" required type="email" placeholder="Email" className="input"/><input name="phone" placeholder="Phone / Teléfono" className="input"/></div><div className="grid gap-4 sm:grid-cols-2"><input name="country" required placeholder="Country / País / Ülke" className="input"/>{type==="QUOTE"?<input name="quantity" placeholder="Quantity / Cantidad" className="input"/>:<input name="subject" placeholder="Subject / Asunto" className="input"/>}</div><textarea name="message" required rows={6} placeholder="Tell us about your requirement..." className="input resize-none"/><button disabled={state==="loading"} className="btn-primary">{state==="loading"?"Sending...":"Submit"}</button>{state==="success"&&<p className="text-sm text-emerald-700">Your request has been received successfully.</p>}{state==="error"&&<p className="text-sm text-red-700">Something went wrong. Please try again.</p>}</form>}
