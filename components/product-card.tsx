import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight, MapPin } from "lucide-react";
import type { Locale } from "@/lib/i18n";
export function ProductCard({product,locale}:{product:any,locale:Locale}){return <article className="group overflow-hidden rounded-[1.75rem] border border-ink/8 bg-white shadow-[0_20px_60px_rgba(8,20,39,.06)] transition duration-500 hover:-translate-y-1.5 hover:shadow-[0_28px_80px_rgba(8,20,39,.13)]"><div className="relative h-60 overflow-hidden"><Image src={product.image} alt={product.names[locale]} fill className="object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-ink/60 via-transparent to-transparent"/><span className="absolute left-4 top-4 rounded-full border border-white/20 bg-ink/55 px-3 py-1 text-[10px] font-semibold uppercase tracking-wider text-white backdrop-blur">{product.category}</span><span className="absolute bottom-4 left-4 flex items-center gap-1.5 text-xs font-medium text-white"><MapPin size={13}/>{product.origin}</span></div><div className="p-6"><h3 className="text-xl font-semibold tracking-tight">{product.names[locale]}</h3><p className="mt-3 line-clamp-2 text-sm leading-6 text-ink/58">{product.desc[locale]}</p><Link className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-ocean transition group-hover:text-copper" href={`/${locale}/products/${product.slug}`}>{locale==="tr"?"Detayları incele":locale==="es"?"Ver detalles":"View details"}<ArrowUpRight size={16}/></Link></div></article>}
