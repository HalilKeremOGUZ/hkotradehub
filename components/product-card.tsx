import Image from "next/image"; import Link from "next/link"; import { ArrowUpRight } from "lucide-react"; import type { Locale } from "@/lib/i18n"; export function ProductCard({product,locale}:{product:any,locale:Locale}){return <article className="card overflow-hidden"><div className="relative h-56"><Image src={product.image} alt={product.names[locale]} fill className="object-cover"/></div><div className="p-6"><div className="flex justify-between text-xs uppercase tracking-wider text-ink/45"><span>{product.category}</span><span>{product.origin}</span></div><h3 className="mt-3 text-xl font-semibold">{product.names[locale]}</h3><p className="mt-3 text-sm leading-6 text-ink/60">{product.desc[locale]}</p><Link className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-copper" href={`/${locale}/products/${product.slug}`}>Details <ArrowUpRight size={16}/></Link></div></article>}